"use strict";Object.defineProperty(exports, "__esModule", {value: true});// src/utils/toUrlSafe.ts
function toUrlSafe(str) {
  let safeStr = str;
  while (safeStr.indexOf("/") >= 0) {
    safeStr = safeStr.replace("/", "_");
  }
  while (safeStr.indexOf("+") >= 0) {
    safeStr = safeStr.replace("+", "-");
  }
  while (safeStr.indexOf("=") >= 0) {
    safeStr = safeStr.replace("=", "");
  }
  return safeStr;
}



exports.toUrlSafe = toUrlSafe;
//# sourceMappingURL=chunk-2JBODGXX.cjs.map