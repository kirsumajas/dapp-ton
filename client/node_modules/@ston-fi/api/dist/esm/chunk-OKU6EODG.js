// src/utils/toUrlSafe.ts
function toUrlSafe(str) {
  let safeStr = str;
  while (safeStr.indexOf("/") >= 0) {
    safeStr = safeStr.replace("/", "_");
  }
  while (safeStr.indexOf("+") >= 0) {
    safeStr = safeStr.replace("+", "-");
  }
  while (safeStr.indexOf("=") >= 0) {
    safeStr = safeStr.replace("=", "");
  }
  return safeStr;
}

export {
  toUrlSafe
};
//# sourceMappingURL=chunk-OKU6EODG.js.map